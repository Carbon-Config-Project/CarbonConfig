sourceSets {
    main {
        java {
            srcDir "carbonconfig/src/main/java"
        }
    }
    tools {
        java {
            compileClasspath += main.output + main.compileClasspath
            runtimeClasspath += main.output + main.runtimeClasspath
        }
    }
}

configurations.toolsAnnotationProcessor.extendsFrom(configurations.annotationProcessor)

tasks.named('test') {
    failOnNoDiscoveredTests = false
}

task generateLangFiles(type: JavaExec) {
    group = 'internal'
    description = 'Generate lang files'
    classpath = sourceSets.tools.runtimeClasspath
    mainClass = 'carbonconfiglib.LangConverter'
}
